{
  "name": "hermes-web-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Browser e2e/integration gate for the Hermes holospace Pages deployment (Playwright).",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "e2e": "./run.sh",
    "e2e:build": "BUILD=1 ./run.sh",
    "doctor": "./run.sh doctor",
    "install-browser": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "1.61.0"
  }
}
